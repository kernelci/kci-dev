kci_dev_release:
  steps:
    - trigger_services:
        project: home:arisut
        package: kci-dev
  filters:
    event: push
    branches:
      only:
        - main
kci_dev_pull_request:
  steps:
    - trigger_services:
        project: home:arisut
        package: kci-dev
  filters:
    event: pull_request
